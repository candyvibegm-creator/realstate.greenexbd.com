# Real Estate CMS - MC World (GreenexBD)

এটি একটি Laravel-based Real Estate CMS (Botble CMS framework দ্বারা তৈরি)। এই প্রোজেক্টটি ডেভেলপমেন্ট থেকে লাইভ সার্ভারে (Shared Hosting) সেটআপ করার সকল তথ্য নিচে দেওয়া হয়েছে।

---

## 🔐 Credentials (সবচেয়ে গুরুত্বপূর্ণ)

সার্ভারে সেটআপ করার জন্য নিচের তথ্যগুলো ব্যবহার করা হয়েছে:

### 🗄️ Database Credentials
এগুলো `.env` ফাইলে এবং cPanel-এ সেট করা আছে:
- **Database Name:** `greenexb_mcworld_db`
- **Database User:** `greenexb_greenexb_mcworld_db`
- **Database Password:** `McWorld@2026!`
- **DB Host:** `localhost` (127.0.0.1)

### 👨‍💻 Admin Panel Access
- **Admin Login URL:** [https://realestate.greenexbd.com/admin](https://realestate.greenexbd.com/admin)
- **Username:** `admin`
- **Password:** `12345678`

---

## 📂 Project Folder Structure Overview

একজন ডেভেলপার হিসেবে আপনার যে ফোল্ডারগুলো জানা জরুরি:

- `public/`: এটি প্রোজেক্টের মেইন মেগ গেটওয়ে। প্রোজেক্ট রান করার জন্য সাবডোমেইনকে এই ফোল্ডারের ভেতরে পয়েন্ট করতে হয়।
- `platform/themes/flex-home/`: বর্তমানে যে থিমটি (`flex-home`) ব্যবহার হচ্ছে তার সকল Front-end ফাইল এখানে পাবেন।
  - `assets/sass/_custom.scss`: কাস্টম স্টাইল এবং এনিমেশন এখানে লেখা হয়েছে।
  - `partials/short-codes/`: বিভিন্ন সেকশনের (যেমন Featured Properties, Carousel) Blade ফাইল এখানে আছে।
- `database/`: ডাটাবেস ব্যাকআপ এবং মাইগ্রেশন ফাইল এখানে থাকে। মেইন ব্যাকআপ ফাইলটি হলো `mcworld_realestate2.sql` যা রুটে রাখা হয়েছে।
- `.env`: অ্যাপ্লিকেশনের সকল সিক্রেট কী, ডাটাবেস কানেকশন এবং ইউআরএল কনফিগারেশন এখানে থাকে।

---

## 🚀 Deployment & Installation Steps (সারসংক্ষেপ)

সার্ভারে সেটআপ করার জন্য নিচের ধাপগুলো অনুসরণ করা হয়েছে:

1. **Upload & Extract:** পুরো প্রোজেক্ট জিপ করে সাবডোমেইনের রুটে আপলোড করা হয়েছে।
2. **Database:** cPanel-এ ডাটাবেস এবং ইউজার তৈরি করে প্রিভিলেজ দেওয়া হয়েছে।
3. **Import SQL:** `mcworld_realestate2.sql` ফাইলটি phpMyAdmin-এর মাধ্যমে ইমপোর্ট করা হয়েছে।
4. **.env Config:** ডাটাবেস ক্রেডেন্সিয়াল এবং `APP_URL` সাবডোমেইন অনুযায়ী সেট করা হয়েছে।
5. **Document Root:** সাবডোমেইনকে `/public` ফোল্ডারে পয়েন্ট করা হয়েছে (সবচেয়ে গুরুত্বপূর্ণ ধাপ)।
6. **Force SSL:** সাইটটিকে সবসময় `https` মোডে রাখার জন্য `.env` ফাইলে `FORCE_SCHEMA=https` করা হয়েছে।

---

## 🛠️ Key Technical Modifications (যা যা করা হয়েছে)

- **2D Item Rotation:** প্রোপার্টি কার্ডগুলোতে একটি স্পিনিং এনিমেশন যোগ করা হয়েছে।
- **Featured Carousel Enhancement:** হোমপেজের প্রধান ক্যারোসেলটিকে Swiper.js-এর মাধ্যমে 'Coverflow' ইফেক্ট দেওয়া হয়েছে যাতে মাঝখানের কার্ডটি বড় দেখায়।
- **SEO Optimization:** টাইটেল ট্যাগ এবং মেটা ডেসক্রিপশনগুলো অপ্টিমাইজ করা হয়েছে।
- **Deployment Ready:** লোকাল এনভায়রনমেন্ট থেকে প্রোডাকশন এনভায়রনমেন্টে সিফট করার সকল কনফিগারেশন করা হয়েছে।

---

## ⚡ Important Commands (যদি প্রয়োজন হয়)

যদি নতুন কোন ফাইল আপলোড করার পর বা কনফিগারেশন চেঞ্জ করার পর সাইটে সমস্যা হয়, তবে নিচের কমান্ডগুলো (Terminal-এ) কাজে লাগতে পারে:

```bash
# Cache ক্লিয়ার করার জন্য
php artisan optimize:clear

# Storage লিঙ্ক করার জন্য (ছবি না দেখা গেলে এটি করবেন)
php artisan storage:link
```

---

## 📝 Developer Notes (দৃষ্টি আকর্ষণ)

- **PHP Version:** এই প্রোজেক্টটি PHP 8.2 বা তার উপরের ভার্সনে ভালো কাজ করবে।
- **SASS Compilation:** `_custom.scss` ফাইলে কোন চেঞ্জ করলে তা কম্পাইল করে `public/themes/flex-home/css/style.css` এ আপডেট করতে হবে।
- **Permissions:** সার্ভারে `storage` এবং `bootstrap/cache` ফোল্ডারের রাইট পারমিশন (775/777) থাকতে হবে।

---
